/**
 * 2つの数値の合計を返します。
 * @param a - 最初の数値。
 * @param b - 二番目の数値。
 * @returns a と b の合計。
 */
export function sum(a, b) {
  return a + b;
}

/**
 * 2つの数値の平均を返します。
 * @param a - 最初の数値。
 * @param b - 二番目の数値。
 * @returns a と b の平均。
 */
export function average(a, b) {
  return sum(a, b) / 2;
}
